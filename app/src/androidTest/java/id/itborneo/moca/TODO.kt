package id.itborneo.moca

import androidx.test.ext.junit.runners.AndroidJUnit4
import org.junit.runner.RunWith

/**
 * Instrumented test, which will execute on an Android device.
 *
 * See [testing documentation](http://d.android.com/tools/testing).
 */
@RunWith(AndroidJUnit4::class)
class TODO {
    /**
     *
     * Movie Fragment
     * - can open movie fragment
     * - can show list movie
     * - cek if list not 0
     * - can scroll
     * - can click detail
     *
     * Series Fragment
     * - can open movie fragment
     * - can show list movie
     * - cek if list not 0
     * - can scroll
     * - can click detail
     *
     *
     * Home Fragment
     * - can open Home fragment
     * - can show list trending movie
     * - can click detail
     *
     *
     * - can show list trending series
     * - can click detail
     *
     * * - can show list Now Playing
     * - can click detail
     *
     * * - can show list Airing today
     * - can click detail
     */

//    @Test
//    fun useAppContext() {
//        // Context of the app under test.
//        val appContext = InstrumentationRegistry.getInstrumentation().targetContext
//        assertEquals("id.itborneo.moca", appContext.packageName)
//    }
}